LOG_LEVEL=info
POSTGRES_HOST=localhost
POSTGRES_USER=demo
POSTGRES_PASSWORD=changeme
POSTGRES_DB=demo
REDIS_HOST=localhost
REDIS_LOG_LEVEL=notice
API_HOST=localhost
FRONTEND_TITLE="Rumor Fullstack Example"
# Note: POSTGRES_PORT, REDIS_PORT, API_PORT, and FRONTEND_PORT are NOT set
# here. They are declared as dynamicPorts in fullstack.config.json, so rumor
# allocates them per checkout and persists them in .rumor-ports.json.

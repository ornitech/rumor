# Local override: bump log level to debug for the api service only.
# Central ../.env sets LOG_LEVEL=info; this file wins because envFiles are
# loaded after the central .env (later files override earlier ones).
LOG_LEVEL=debug

# Local override for the frontend service. Central ../.env defines
# FRONTEND_TITLE; this file overrides it. NOTE: LOG_LEVEL for this service
# is overridden a third time by the `env` block in fullstack.config.json
# (config-block env wins over both file layers).
FRONTEND_TITLE="Frontend (local override)"

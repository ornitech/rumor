# This is a standalone open-source project: use the public npm registry rather
# than any corporate proxy configured globally.
registry=https://registry.npmjs.org/
always-auth=false
engine-strict=false
